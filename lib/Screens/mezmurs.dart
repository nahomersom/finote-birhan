import 'package:flutter/material.dart';
class Mezmurs extends StatelessWidget{
  Widget build(BuildContext context){
    return Scaffold(
      body: Center(
        child: Text('hi',style: TextStyle(
         fontWeight: FontWeight.bold
        ),),
      ),
    );
  }
}